name: Analytics
on: push

jobs:
  pester-test:
    name: Validation
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v3
      - name: Validate Sentinel Analytics Rules
        uses: SecureHats/private@v4.12.3-alpha
        with:
          path: = "templates"
